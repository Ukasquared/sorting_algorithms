#include "sort.h"

/**
 * quick_sort - Entry point
 *
 * @array: the data set to search
 * @size: size of data set
 * Return: Always 0
 */

void quick_sort(int *array, size_t size)
{

	int pivot, i, j;

	if (size < 2)
		return;

	pivot = array[size - 1]
	
	for (i = 0; i < size - 1; i++)
	{
		for (j = 0; j < 

}
